const http = require('http'); http.get('http://localhost:3000/lms/admin', (res) => { let data = ''; res.on('data', (chunk) => { data += chunk; }); res.on('end', () => { console.log('Status:', res.statusCode); console.log('Body length:', data.length); if (res.statusCode === 500) { console.log('Error data:', data); } }); }).on('error', (err) => { console.log('Error: ' + err.message); });
